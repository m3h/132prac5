#ifndef NUMBER_ARRAY_H
#define NUMBER_ARRAY_H


void reverseOrder( int [], size_t size );

void rotate( int [], size_t size, int dist );

bool shift( int array[], size_t size, int startInd, int stopInd, int dist );

void displayArray( int [], size_t size );


#endif
